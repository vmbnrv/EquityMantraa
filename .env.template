# Copy this to .env and fill in real values on the production server
# Never commit .env to git—it should be created manually on each host

# SMTP Configuration
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-smtp-user
SMTP_PASS=your-smtp-password

# Ghost Site URL
GHOST_URL=https://equitymantraa.com

# ACME (Let's Encrypt)
ACME_EMAIL=your-email@example.com
