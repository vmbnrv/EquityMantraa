# Ghost runtime artifacts
letsencrypt/
ghost-content/logs/
ghost-content/data/*.log
# Note: ghost.db IS tracked to preserve admin users and content across deploys

# Node/yarn caches
node_modules/
*.log

# Environment secrets (never commit real credentials)
.env
.env.local
.env.prod

# macOS
.DS_Store
