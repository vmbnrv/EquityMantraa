# Ghost runtime artifacts
letsencrypt/
ghost-content/logs/
ghost-content/data/*.log
ghost-content/data/ghost.db  # Database is NOT tracked - backed up manually to avoid git sync conflicts

# Node/yarn caches
node_modules/
*.log

# Environment secrets (never commit real credentials)
.env
.env.local
.env.prod

# macOS
.DS_Store
